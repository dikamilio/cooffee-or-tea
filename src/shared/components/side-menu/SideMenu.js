import React from "react";
import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    StatusBar,
} from 'react-native';
import styles from "./styles";
const SideMenu = () => {
    return (
        <View style={styles.sideMenu}>
            <Text>Step One</Text>
        </View>
    )
}

export default SideMenu;