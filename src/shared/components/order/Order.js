import React from "react";
import {
    View,
    Text,
    TouchableOpacity
} from 'react-native';
import styles from "./styles";

const Order = () => {
    return (
        <View style={styles.cartContainer}>
            <Text>elo</Text>
        </View>
    )
};

export default Order;